import { ClrDirective } from './clr.directive';

describe('ClrDirective', () => {
  it('should create an instance', () => {
    const directive = new ClrDirective();
    expect(directive).toBeTruthy();
  });
});
